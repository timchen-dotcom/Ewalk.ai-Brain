# 2026-05-13 GitHub 自我升級情報

> 範圍：近 24 小時～7 天（以 2026-05-13 為基準）  
> 原則：先優先 AI 工具 → 再高度討論 repo → 再通用優化（本次以工具更新/可落地工作流為主）

## 今日總結

- 找到 5 則高價值情報：`#Codex`、`#ClaudeCode`、`#Gemini` 各有可直接改善日常 CLI/Agent 工作流的更新；另 1 則偏「MCP 工具治理」方向，適合做中長期的工具上架/權限控管基礎。
- 建議 **可升級 2 則**（Codex / Gemini CLI），其餘 **先觀察 3 則**（視提姆是否要投入整合/治理成本再決定）。

---

## 值得關注（最多 5 則）

### 1) Codex CLI `0.129.0`：Composer 支援 Vim modal editing（提升長任務輸入效率）

- 來源標題：Releases · openai/codex（`0.129.0`）
- 來源連結：https://github.com/openai/codex/releases
- 相關工具標籤：#Codex #工作流
- 重點摘要：
  - TUI 的 composer 新增 **Vim modal editing**（含 `/vim`、預設模式設定、Vim 專用 keymap context）。
  - 對長任務撰寫（SOP/摘要/規格/多段 prompt）時，能把編輯效率拉回到熟悉的 Vim 流程。
- 對 Ewalk.ai 的白話幫助：
  - 阿順在 Codex composer 打「長段落、長清單、結構化輸出」時，會更快、更不容易打亂格式（尤其是 Obsidian 版型）。
  - 若後續要把每日情報自動化流程常駐化（remote-control 試點），編輯體驗更穩定，降低人為操作成本。
- 建議狀態：**可升級**（提姆允許後，安排一次小回歸：輸入/編輯/貼上/多段產出是否穩定）

---

### 2) Claude Code Week 19（2026-05-04～05-08）：zip/URL 載入插件 + 跨專案歷史搜尋

- 來源標題：Week 19 · May 4–8, 2026 - Claude Code Docs
- 來源連結：https://code.claude.com/docs/en/whats-new/2026-w19
- 相關工具標籤：#ClaudeCode #工作流
- 重點摘要：
  - 插件可直接從 `.zip` 或 URL 載入（`--plugin-dir` 支援 zip、`--plugin-url` 可從 URL 抓 zip）。
  - `Ctrl+R` 可跨「所有專案」搜尋 command history。
  - auto mode 新增 hard deny 規則（可無條件阻擋行為）。
- 對 Ewalk.ai 的白話幫助：
  - 若未來要做「內部插件/技能包」快速試用與回收（不正式上架），zip/URL 載入很省事。
  - 跨專案歷史搜尋適合在多客戶、多 repo 的情境快速複用命令套路（例如固定的測試、lint、產出流程）。
- 建議狀態：**先觀察**（除非提姆確定 Claude Code 會成為日常主力 CLI，才值得投入插件分發流程）

---

### 3) Gemini CLI（近 7 天 release）：修 OAuth headless hang、改善 untrusted folder 的 MCP list UX

- 來源標題：Releases · google-gemini/gemini-cli（v0.42.0 nightly / preview）
- 來源連結：https://github.com/google-gemini/gemini-cli/releases
- 相關工具標籤：#Gemini #工作流 #省Token
- 重點摘要（近 7 天內較「可直接省時間」的變更）：
  - 修正 headless Linux OAuth 可能「無聲卡住」（避免自動化跑一半卡死）。
  - 改善 untrusted folders 下 `mcp list` 的 UX（更貼近安全/信任模型的操作手感）。
  - CI / macOS binary 可執行檔權限等穩定性修復（降低安裝/升級踩雷）。
- 對 Ewalk.ai 的白話幫助：
  - 若要把 Gemini CLI 當「長上下文 + Google 生態」的工作流補位工具（例如長文件理解、跨工具協作），這些修復會直接提升可用性與穩定性。
  - `mcp list` UX 變好，代表它更適合納入「工具治理/信任資料夾」的規範化流程。
- 建議狀態：**可升級**（提姆允許後，安排一次「自動化模式/登入/跑 MCP」的最小回歸）

---

### 4) Claude Platform（2026-05-06）：Managed Agents Multiagent sessions + Webhooks（偏平台級能力）

- 來源標題：Claude Platform - Release notes（May 6, 2026）
- 來源連結：https://platform.claude.com/docs/en/release-notes/overview
- 相關工具標籤：#ClaudeCode #工作流 #自我升級
- 重點摘要：
  - Managed Agents 的 multiagent sessions / Outcomes 進入 public beta（需指定 beta header）。
  - 支援 Managed Agents webhooks（session / vault lifecycle events），並加強事件/會話的 filter/sort。
- 對 Ewalk.ai 的白話幫助：
  - 這比較像「把 agent 變成可監控、可串接事件」的基礎能力：如果未來 Ewalk.ai 想把 agent 執行狀態丟回內部系統（例如產檔完成、失敗重試、權限到期），webhook 是關鍵拼圖。
- 建議狀態：**先觀察**（除非提姆要投入「平台級 agent 監控/稽核」，否則先記錄即可）

---

### 5) 高度討論 repo：`agentic-community/mcp-gateway-registry`（MCP 工具治理：OAuth、Registry、動態工具發現）

- 來源標題：agentic-community/mcp-gateway-registry
- 來源連結：https://github.com/agentic-community/mcp-gateway-registry
- 相關工具標籤：#高度討論Repo #開源工具 #工作流
- 熱度 / 討論理由（依 repo 公開訊號）：
  - 星數/分支：約 **637 stars / 164 forks**（可視為有一定社群採用與轉載）。
  - 近期 release：**May 7, 2026** 有新版（v1.0.22）並持續迭代。
  - 主題直指 Ewalk.ai 目前最缺的一塊：MCP server 混亂後的「治理、OAuth、動態工具清單、可稽核」。
- 重點摘要：
  - 目標是把分散的 MCP servers 工具接入，變成「集中式 Gateway + Registry」：有 OAuth、動態工具發現、統一存取、可管控。
  - 企業常見需求（Keycloak/Entra 等身分/群組）是它主打方向。
- 對 Ewalk.ai 的白話幫助：
  - 如果未來我們 MCP 工具數量變多（GitHub、Notion、Slack、資料庫、素材系統…），這類 Gateway/Registry 會決定「哪些 agent 能用哪些工具」與「出事怎麼追」。
  - 但它不是「今天就能省 2 小時」的工具，偏長期工程：要有一套工具上架、權限、稽核流程才會回本。
- 建議狀態：**先觀察**（等提姆確認是否要做 MCP 工具治理專案，再進一步做 PoC）

---

## 對 Ewalk.ai 的白話幫助（提姆版）

1. **Codex**：更新後 composer 變成「可 Vim 編輯」，長任務輸出更快、更穩定。  
2. **Gemini CLI**：近 7 天修了 OAuth 卡死、改善 MCP list（配合信任資料夾的安全模型），更適合拿來補位長上下文/多模態與 Google 生態。  
3. **MCP 治理**：如果我們 MCP 工具越來越多，`mcp-gateway-registry` 這種「工具治理層」會變重要，但要做就要當成小專案來做。

## 建議是否升級

- 可升級（2）：
  - Codex CLI `0.129.0`（Vim modal editing）
  - Gemini CLI（近 7 天 release：OAuth hang 修復、MCP list UX）
- 先觀察（3）：
  - Claude Code Week 19（除非要把 Claude Code 變主力）
  - Claude Platform Managed Agents（平台級能力，先記錄）
  - `mcp-gateway-registry`（等要做工具治理再 PoC）

## Discord 通知草稿

阿順自我升級情報｜2026-05-13

今天找到 5 則值得看：2 則建議可升級、3 則先觀察。

最有用的 3 件事：
1) Codex CLI `0.129.0`：composer 支援 Vim 編輯，長任務輸出更快更穩。
2) Gemini CLI（近 7 天）：修 OAuth headless 卡死、改善 MCP list（更好配合信任資料夾/安全模型）。
3) 高度討論 repo `mcp-gateway-registry`：開始有「MCP 工具治理層」的雛形（OAuth + Registry + 動態工具發現），適合未來做工具權限控管。

白話說，這對 Ewalk.ai 的幫助是：把日常 CLI/Agent 工作流更穩、更快，並開始補「工具治理」這塊長期缺口。

我建議：
- 可升級：Codex `0.129.0`、Gemini CLI（近 7 天 release）
- 先觀察：Claude Code Week 19、Claude Platform Managed Agents、mcp-gateway-registry（等要做治理再 PoC）

等提姆允許後，我再進入 /自我升級。

## 待提姆確認

- 是否允許把 Codex 升到 `0.129.0`，並依「Codex CLI 升級與回歸驗證 SOP」跑一次最小回歸？
- 是否允許把 Gemini CLI 也納入工具清單（做一次登入 + MCP + headless 流程最小回歸）？
- MCP 工具治理是否要立項（若要，我再把 `mcp-gateway-registry` 整理成 PoC 方案與成本預估）？

